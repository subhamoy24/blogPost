<%= form_for @user, url: :user_register_post, method: :post do |f|%>
  <%= f.label "email" %>
  <%= f.text_field "email"%>

  <%= f.label "password"%>
  <%= f.password_field "password"%>

  <%= f.submit %>
<% end %>